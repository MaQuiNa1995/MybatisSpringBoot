create table Planeta
(
   id integer not null,
   nombre varchar(255), 
   fauna varchar(255),
   centinelas varchar(255),
   flora varchar(255),
   clima varchar(255),
   temperatura float,
   lunas integer,
   primary key(id)
);